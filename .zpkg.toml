[package]
org = "zed-pkg-test"
name = "security-path-traversal"
version = "0.1.0"
description = "Independent security acceptance harness"
license = "MIT"

[package.repository]
vcs = "git"
url = "https://github.com/zed-pkg-test/security-path-traversal"

[install]
dir = ".vendor/.zed"

[publish]
exclude = ["artifacts/**", "vendor/**", ".vendor/**", ".env", ".env.*"]
