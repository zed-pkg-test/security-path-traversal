[build-system]
requires=["setuptools>=68"]
build-backend="setuptools.build_meta"
[project]
name="security-path-traversal-harness"
version="0.1.0"
requires-python=">=3.11"
dependencies=["PyYAML>=6,<7"]
[project.optional-dependencies]
test=["pytest>=8,<10"]
[tool.pytest.ini_options]
addopts="-ra"
testpaths=["tests"]
