schema_version: 1
managed_by: github-test-org-factory/1.0.0
production_org: zed-pkg
test_org: zed-pkg-test
repository: security-path-traversal
kind: security
readiness: ready
objectives:
- Verify archive traversal, unsafe symlinks, checksum/provenance mismatches, and credential redaction across the supported
  happy-path states and canonical fixtures.
- Verify archive traversal, unsafe symlinks, checksum/provenance mismatches, and credential redaction under retries,
  interruption, concurrency, offline operation, or partial failure.
- Verify archive traversal, unsafe symlinks, checksum/provenance mismatches, and credential redaction preserves authorization,
  idempotency, integrity, observability, and actionable failure classification.
always_on_checks:
- Validate test-plan and dependency metadata.
- Validate generated fixture and workflow syntax.
- Classify missing upstreams or credentials separately from product regressions.
schedule_utc: 23 4 * * 2,5
expensive: false
required_infrastructure: []
cases:
- id: TC-001
  title: Verify archive traversal, unsafe symlinks, checksum/provenance mismatches, and credential redaction across the
    supported happy-path states and canonical fixtures.
  tier: contract-live
  status: planned
  evidence:
  - junit
  - readiness JSON
  - workflow artifacts
- id: TC-002
  title: Verify archive traversal, unsafe symlinks, checksum/provenance mismatches, and credential redaction under retries,
    interruption, concurrency, offline operation, or partial failure.
  tier: contract-live
  status: planned
  evidence:
  - junit
  - readiness JSON
  - workflow artifacts
- id: TC-003
  title: Verify archive traversal, unsafe symlinks, checksum/provenance mismatches, and credential redaction preserves
    authorization, idempotency, integrity, observability, and actionable failure classification.
  tier: contract-live
  status: planned
  evidence:
  - junit
  - readiness JSON
  - workflow artifacts
